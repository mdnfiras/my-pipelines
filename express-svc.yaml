apiVersion: v1
kind: Service
metadata:
  name: express-svc
  namespace: web-app
spec:
  type: NodePort
  selector:
    app: express-app 
  ports:
    - nodePort: 40001
      port: 3000
      targetPort: 3000
